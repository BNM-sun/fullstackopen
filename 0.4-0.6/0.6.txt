sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note in the SPA and clicks save

    browser->>server: POST /new_note_spa (JSON data)
    server-->>browser: 201 Created

    Note right of browser: JavaScript updates the page with the new note
    browser->>server: GET /data.json (optional)
    server-->>browser: JSON data (updated notes)
